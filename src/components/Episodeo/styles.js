import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginBottom: 15,
    },
    row: {
        flexDirection: 'row',
        marginBottom: 10,
    },
    capa: {
        width: 160,
        height: 100,
        marginRight: 10,
    }
});

export default styles;